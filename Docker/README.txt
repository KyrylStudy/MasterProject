Build and run docker containers(run in command line in this directory):

docker compose up --build --force-recreate


After first run you have to create an Architekture. Only after that you can create hardware and connect it.